export enum Languages {
    English = "en",
    VietNamese = "vi",
}

export enum DeviceType {
    Mobile = "mobile",
    Tablet = "tablet",
    Desktop = "desktop",
}

export enum APIStatus {
    SUCCESS = "success",
    ERROR = "error",
}
